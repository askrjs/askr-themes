import "../../../src/themes/default/index.css";
import "../../../src/themes/presets/index.css";

/**
 * Loads the default theme plus every preset. The audit builds plain DOM rather
 * than components, so the spec constructs and measures it directly.
 */
export default function themeStylesheets(): void {}
